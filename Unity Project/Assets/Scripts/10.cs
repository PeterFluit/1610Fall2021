using System.Collections;
using System.Collections.Generic;
using UnityEngine;

double myNum = 19.99D;
Console.WriteLine(myNum);

public class 10 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
